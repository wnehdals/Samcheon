# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**Samcheon** — a Python project, currently in early setup stage.

## Language Preference

모든 응답과 코드 주석은 한국어로 작성한다. 예외 메시지 등 기술적 문자열은 원문 유지.

### 에이전트 라우팅

코드 작성 시 pyqt-expert-agent, python-expert-agent를 선택한다.


## Running

```bash
python main.py
```

## Environment

- Python (PyCharm project, `.gitignore` includes support for venv, uv, poetry, pdm, pipenv)
- No dependencies, test framework, or build system defined yet
